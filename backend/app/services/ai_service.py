# This file contains placeholder/stub functions to integrate LangChain/LangGraph later.

def simple_search(query: str) -> str:
    # For now return a canned response. Later, you'll:
    # - load product data into a vector DB
    # - use LangChain LLM + retriever to answer queries
    # - orchestrate multi-step tasks with LangGraph
    return f"[AI stub] I received your query: {query}. Connect LangChain to provide real answers."
